<!DOCTYPE html>
<html lang="en">
​
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="../styles/base.css">
​
</head>
​
<body class="bodyClass">
    <header>
        <%- include('../../layout/nav') %>
    </header>
    <div class="loops">
        <div class="loopContent">
            <h2> There are <%= books.length %> Books</h2><br>
            <h4>Title: <%= books.title %></h4><br>
            <p>Author: <%= books.author %></p><br>
            <img src="<%= books.image_url %>"><br>
            <p>ISBN: <%= books.isbn %></p><br>
            <p>Description: <%= books.description %></p><br>
​
            <button id='showUpdateForm'>Update Book</button><br>
            <form action="/deleteBook/<%=books.id%>?_method=delete" method="post"><br>
            <button id='deleteBook'>Delete Book</button><br>
        </form>
​
        </div>
    </div>   
    
    <!-- we add the type of the CRUD either put -->
    <form  action="/UpadateBookInfo/<%=books.id%>?_method=put" method="post" id="updataForm">
        <input type="text" name="title" value=<%= books.title %>>
        <input type="text" name="author" value="<%= books.author %>">
        <input type="text" name="image_url" value="<%= books.image_url %>">
        <input type="text" name="isbn" value="<%= books.isbn %>">
        <textarea name="description" cols="30" rows="10"> <%= books.description %> </textarea>
        <button type="submit">Update My data</button>
    </form>
​
​
​
​
    <script src="https://code.jquery.com/jquery-3.5.1.js"></script>
    <script src="/js/app.js"></script>
</body>
​
</html>